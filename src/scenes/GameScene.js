import Phaser from "phaser";


class GameScene extends Phaser.Scene {
    constructor(test) {
        super({
            key: "GameScene",
        });
    }

    preload() {
        //code
    }

    create() {
        //code
    }

    update(delta, time) {
        //code
    }
}
export default GameScene;

